//Ryan Hoffman
//AP Java Period 5
import java.util.*;
public class Contact {
	
	public static Person[] deleteContact(int contactNum) {
		Person[] array = new Person[3];
		//Method accepts an int parameter. Array[contactNum] values are set to null or 0.
		return array;
	}
	public static void addContact(String name, int number, String email) {
		Person c = new Person(name, number, email);
		System.out.println(c.toString()+"\n");
	}
	public static int binarySearch(char[] a, int fromIndex, int toIndex, char key) {
		//User inputs a letter. Finds array indexes that contain the name that starts with that letter and prints it.
		return toIndex;
	}
	public static Person[] sort(Person[] array) {
		//Method accepts Person array as parameter and sorts alphabetically by name using sort from api.
		return array;
	}
	//If we could go over the process of making this class that would be great.
	
	
	
}
